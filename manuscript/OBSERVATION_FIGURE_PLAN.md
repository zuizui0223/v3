# OBSERVATION figure architecture

Status: figure-production plan for `manuscript/OBSERVATION_DRAFT_V1.md`.

## Figure 1 — Three observation operators on compatible worlds

Purpose: establish the paper's conceptual identity without importing Evidence-owned design claims.

Panel A — Refinement

```text
primary record E
compatible worlds {w1,w2,w3}
        + retained side channel R
                -> {w1,w2}
```

Panel B — Record-entry selection

```text
opportunity universe
  entered rows + shadow/no-row
        selected event table
```

Show that two different shadow compositions can map to the same selected table.

Panel C — Semantic coarsening

```text
rich retained evidence
B / T / N / U
        -> deterministic binary mapping
TARGET / not-TARGET
```

Show fibre merging rather than classifier confusion.

Panel D — Irreversibility rule

```text
lost distinction + downstream function only -> cannot recover
lost distinction + independent/new channel  -> may recover
```

No Boundary/MROD/CED next-measurement arrow in Figure 1; that belongs to Evidence.

## Figure 2 — Strict refinement in the frozen V3 synthetic system

Primary panels:

A. balanced utility: matched reference `0.8327`, no reference `0.5688`, time-permuted `0.7301`;
B. nuisance false-frame rate: matched `0.0272`, no reference `0.2986`;
C. local-sway false-frame rate: matched `0.109`, no reference `1.000`;
D. robustness inset: lag1 balanced utility `0.7842`, partial75 `0.7836`.

Caption boundary:

- controlled synthetic strict refinement;
- no physical or universal reference-benefit claim;
- time-broken control demonstrates that channel presence alone is insufficient.

## Figure 3 — Record-entry selection changes a real ecological estimand

This is the main reality-facing figure.

Panel A — fox/badger pipeline

```text
881 CCTV-confirmed passes
      -> confirmed trigger
      -> confirmed capture
```

Annotate badger composition:

- reference `0.3598`;
- trigger `0.4393`;
- capture `0.4828`.

Panel B — position-standardized shifts

Trigger:

- equal-position `+0.0624`;
- reference-pass-weighted `+0.0535`.

Capture:

- equal-position `+0.1498`;
- reference-pass-weighted `+0.1399`.

Show four position directions and retain SF as adverse.

Panel C — otter context-dependence inset

- A robust negative wet shift;
- BV mostly negative;
- BS approximately zero after standardization.

Purpose: prevent a universal biological-state entry-propensity reading.

## Figure 4 — Upstream irreversibility and transport-limited recovery

Panel A — BirdVox contrast

- true late-minus-early `+0.130820`;
- oracle true-entry-only `-0.000025`.

Visual statement: semantic perfection among retained rows does not recreate omitted truth-positive rows.

Panel B — correction transport ladder

| regime | raw MAE | corrected MAE | result |
|---|---:|---:|---|
| otter matched camera holdout | 0.115982 | 0.059258 | improves |
| fox/badger trigger position holdout | 0.123093 | 0.090510 | improves on average |
| fox/badger capture position holdout | 0.210807 | 0.167972 | improves on average |
| otter camera+position double holdout | 0.068216 | 0.081237 | worsens |

Purpose: make the transport boundary a main result rather than a buried limitation.

## Figure 5 — Semantic coarsening widens the target identified set

Panel A — observation vocabularies

```text
B/T/N/U -> TARGET/not-TARGET
```

Panel B — median identification width

- rich B/T/N/U `~0.030`;
- binary `~0.266`.

Panel C — relative reduction distribution or simplex summary

- median relative reduction `~84.45%` among non-zero binary widths.

Panel D — specificity control

Show that additional non-redundant regime structure, not unique verbal semantics of the frozen U reasons, drives the further narrowing.

Caption boundary: closed-world known-truth stress test, not field prevalence accuracy.

## Supplementary figures

S1 — exact U1–U3 finite-world witnesses.
S2 — V3 temporal robustness details.
S3 — full fox/badger position table and otter A/BS/BV standardization.
S4 — recovery sham controls and cell-level double-holdout results.
S5 — TNOA low-target / weighting-sensitivity and D5 specificity controls.

## Production rule

Every plotted number must be generated from or validated against a pinned source artifact. Do not manually re-enter scientific values into drawing software without a machine-readable sidecar. Conceptual geometry may be drawn manually, but quantitative panels must have source-data files and a validator before submission.
