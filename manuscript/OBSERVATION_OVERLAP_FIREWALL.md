# OBSERVATION cross-manuscript overlap firewall

Status: submission-production rule under the two-paper architecture.

## Purpose

Observation integrates source-owned results from V3, REC and TNOA. The old standalone manuscripts remain provenance/source documents but are no longer active independent submission targets. This firewall prevents duplicate novelty claims, duplicate abstracts, and accidental simultaneous-submission framing.

## Active manuscript

Only the following manuscript is active for the Observation submission path:

- `zuizui0223/v3/manuscript/OBSERVATION_DRAFT_V1.md`

The following are **source/provenance only** under the current architecture:

- V3 Layer-1/theory drafts;
- `zuizui0223/rec/MANUSCRIPT_DRAFT_H1_H5.md`;
- `zuizui0223/tnoa/manuscript/TNOA_MEE_DRAFT.md`.

Do not submit, preprint, or circulate those source manuscripts as concurrent independent papers without first revoking the two-paper architecture.

## Claim ownership rules

### V3-derived text

May be reused only for:

- retained augmentation/refinement;
- reversible decomposition versus destructive suppression;
- strict synthetic temporal-reference result;
- information-order framing.

Do not import future-observation/MROD novelty into Observation.

### REC-derived text

May be reused only for:

- record-entry selection;
- external denominator/reference requirement;
- fox/badger position-robust estimand distortion;
- BirdVox irreversibility;
- correction/transport hierarchy.

In Observation, these results must be framed as the **reality-facing validation of the support-selection operator**, not as a separate five-hypothesis REC paper.

### TNOA-derived text

May be reused only for:

- semantic preservation/coarsening;
- non-complementary evidence semantics as one worked process-preserving interface;
- D1/D4 identification-width stress test;
- D5 specificity/adverse control.

In Observation, do not retain TNOA's standalone threshold-portability narrative as a headline result unless needed as measurement-calibration support. Do not claim unique semantic optimality for B/T/N/U or the frozen U reasons.

## Abstract firewall

The Observation abstract must not reuse the source-manuscript opening/closing sentences verbatim.

Observation's abstract identity is:

> ecological observation systems perform different information operations before inference: retained augmentation can refine, row-entry selection can delete support, and semantic coarsening can merge distinctions.

REC source identity remains:

> record-entry selection can distort an ecological estimand and correction has a transport domain.

TNOA source identity remains:

> a process-preserving observation interface avoids premature semantic collapse.

These source identities can support Observation but are no longer active separate submission pitches.

## Evidence-paper firewall

Observation may end with a compatible-world / identified set. It may not claim novelty for:

- `k-rank(M)` current mechanism boundary;
- MROD mechanism-information optimization;
- CED target-safe quotient;
- target-report false-resolution/cost contracts;
- adaptive evidence stopping.

Use one handoff sentence only:

> Observation determines the retained compatible-world set; Evidence asks what that set licenses and what additional measurement should be acquired next.

## Self-citation / provenance policy

Before submission:

1. pin the exact source commit/hash for every imported numerical result;
2. merge source bibliographies into one Observation bibliography while removing duplicate entries;
3. cite original external datasets/literature directly rather than citing an unpublished source manuscript as evidence;
4. keep repository-to-repository provenance in the code/data statement or Supplement, not as manuscript novelty;
5. if any source manuscript has already been posted publicly before Observation submission, disclose the relationship explicitly to the editor and cite that public version as required.

## Submission gate

Observation is not ready for journal upload until all of the following are true:

- [ ] active abstract is unique to Observation;
- [ ] no standalone REC/TNOA/V3 manuscript remains marked as an active concurrent submission target;
- [ ] all imported result numbers are pinned to source artifacts;
- [ ] bibliography uses original literature/data provenance;
- [ ] Evidence-owned claims are absent from headline Results and Discussion;
- [ ] journal cover letter states that the manuscript integrates previously separate repository components into one non-overlapping Observation paper.
